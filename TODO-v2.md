# Pricing Logic Overhaul TODO (Approved)

1. [ ] pricing.ts: Add packageTier Input, multipliers, rewrite calculate() (baseMid + addons * multiplier).
2. [ ] ResultPanel.tsx: Tier bar clickable (onSelectTier), package row in breakdown, recommended diff.
3. [ ] Index.tsx: packageTier state, pass onSelectTier/onSelectRecommended.
4. [ ] Test: Custom base ~580€ budget, recommended ~725€ (+145€), steps logical.
5. [ ] Lint/build.

**Progress updated after each.**

